import { FocusServicePage, serviceMetadata } from "@/components/focus-service-page";

export const metadata = serviceMetadata("advisory-growth");

export default function Page() {
  return <FocusServicePage slug="advisory-growth" />;
}
